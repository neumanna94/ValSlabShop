export class Slab {
  constructor (public species: string, public length: number, public width: number, public depth: number, public price: number, public description: string, public imgUrl: string) { }
}
// (species, length, width, depth, price, description, imgUrl)
